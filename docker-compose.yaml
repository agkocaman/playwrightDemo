version: '3.8'
services:
  playwright-test:
    image: playwright-test
    build:
      context: .
      dockerfile: ./Dockerfile
    command: npm run pw:pilot
    volumes:
      - ./playwright-report/:/app/playwright-report
      - ./allure-results/:/app/allure-results